Distribution URL is set in gradle/wrapper/gradle-wrapper.properties.

gradle-wrapper.jar is intentionally NOT committed here (binary).
Android Studio downloads it automatically on first sync.
Or run once:  gradle wrapper --gradle-version 8.11.1
